# Provider configs are entered via the frontend settings UI.
# These env vars serve as a single-provider fallback if no
# providers are sent from the frontend.

AI_BASE_URL=https://api.hncsec.com/v1
AI_API_KEY=sk-your-key-here
IMAGE_MODEL=inkling-v1

TEXT_MODEL=gpt-4o-mini
MAX_OUTPUT_TOKENS=50
REQUEST_TIMEOUT_SECONDS=15
